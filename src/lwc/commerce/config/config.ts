import { currentRelease } from './currentRelease';

const currentCartName = 'current';

const checkoutCartStatus = 'Checkout';

const defaultSortOrder = 'CreatedDateDesc';

export { currentCartName, currentRelease, checkoutCartStatus, defaultSortOrder };
