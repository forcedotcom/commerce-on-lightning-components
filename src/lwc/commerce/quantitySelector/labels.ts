import rangeOverflow from '@salesforce/label/Commerce_QuantitySelector.rangeOverflow';
import rangeUnderflow from '@salesforce/label/Commerce_QuantitySelector.rangeUnderflow';
import stepMismatch from '@salesforce/label/Commerce_QuantitySelector.stepMismatch';

export {
    /**
     * Error message that is shown when the maximum was exceeded.
     */
    rangeOverflow,
    /**
     * Error message that is shown when the minimum was violated.
     */
    rangeUnderflow,
    /**
     * Error message that is shown when the step size was not respected.
     */
    stepMismatch,
};
